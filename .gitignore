# Dados pessoais do usuário — nunca versionar
memoria.md
iscas-local.md
user-memory.md
user-profile.md

# Sistema
.DS_Store
Thumbs.db
*.tmp

# Configurações locais do Claude Code
.claude/

# Repositórios independentes (têm GitHub próprio)
obsidian-zettelkasten/

# Artefatos gerados (scripts/empacotar-zips.*) — não versionar, recriar quando precisar
dist/

# Zips de cada skill para upload manual no claude.ai (Claude Desktop) —
# distribuídos aos alunos fora do GitHub, não fazem diferença pro funcionamento da skill
*.zip

# Planejamento interno (sistema "superpowers"/GSD) — não é skill de aluno,
# não pertence a este repositório público. Mantido só localmente.
docs/superpowers/
# Rascunhos e previews gerados das skills — manter só localmente
**/_draft-redesign/
**/_test-output/
dr-house-triwer/tests/
